%h3
  Dashboard for
  = @restaurant.name
%p Hello
= current_user.email

%div#dash_links
  %ul
    %li
      = link_to 'Create a menu', new_menu_path
    %li
      = link_to 'Edit details', edit_restaurant_path(@restaurant)
    - if @restaurant.menus.any?
      %li
        = link_to 'Create a dish', new_menu_dish_path(@restaurant.menus.first)
